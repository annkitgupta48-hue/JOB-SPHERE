# 🎨 Visual System Architecture & User Flows

## 🏗️ Complete System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    JobSphere Platform                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Frontend Layer (HTML/CSS/JavaScript)                       │
│  ├─ index.html (Homepage)                                   │
│  ├─ login.html (Auth - NEW tabs)                           │
│  ├─ signup.html (Registration)                             │
│  └─ pages/admin-dashboard.html (Admin Panel - NEW)         │
│                                                              │
│  Application Layer (app.js + db.js)                         │
│  ├─ Authentication Functions                               │
│  │  ├─ loginHandler()                                      │
│  │  ├─ adminLogin()          ← NEW                         │
│  │  ├─ signupHandler()                                     │
│  │  └─ handleGoogleCredential()                            │
│  │                                                          │
│  ├─ Job Management Functions                               │
│  │  ├─ getJobs()                                           │
│  │  ├─ saveJobs()                                          │
│  │  ├─ adminPostJob()        ← NEW                         │
│  │  └─ renderAllJobs()       ← NEW                         │
│  │                                                          │
│  ├─ Admin Functions                                         │
│  │  ├─ adminAddCategory()                                  │
│  │  ├─ renderLinkAdmin()                                   │
│  │  ├─ updateStats()                                       │
│  │  └─ [9+ more admin functions]                           │
│  │                                                          │
│  └─ Storage Layer (db.js)                                  │
│     ├─ localStorage (Current)                              │
│     │  ├─ job_spare_users                                  │
│     │  ├─ job_spare_jobs                                   │
│     │  ├─ job_spare_categories                             │
│     │  └─ job_spare_session                                │
│     │                                                       │
│     └─ MongoDB API (When enabled)                          │
│        ├─ /api/jobs (CRUD)                                 │
│        ├─ /api/users (CRUD)                                │
│        └─ /api/categories (CRUD)                           │
│                                                              │
│  External Services                                          │
│  └─ Google OAuth 2.0 ← NEW integration                     │
│     ├─ authenticate via Google account                     │
│     └─ auto-create user profile                            │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 👥 User Authentication Flows

### Flow 1: Email/Password Login (User)

```
┌─────────────────────────────────────┐
│    /pages/login.html                │
│  👤 User Login Tab (default)        │
└──────────────┬──────────────────────┘
               │
               ├─ Input: Email
               ├─ Input: Password
               │
               ↓
         ┌─────────────────┐
         │ Verify User:    │
         │ Find in users[] │
         │ by email        │
         │ & password      │
         └────┬────────────┘
              │
         ┌────┴────────────────┐
         │                     │
      NO │                     │ YES
         │                     │
         ↓                     ↓
    ❌ Error            ✅ setCurrentUser()
    Show msg            Save to sessionStorage
                        │
                        ↓
                  /pages/user-dashboard.html
                  (User profile & jobs)
```

### Flow 2: Admin Login (NEW!)

```
┌─────────────────────────────────────┐
│    /pages/login.html                │
│  🔐 Admin Login Tab (NEW)           │
└──────────────┬──────────────────────┘
               │
               ├─ Input: Admin Email
               ├─ Input: Admin Password
               │
               ↓
         ┌──────────────────┐
         │ Verify Admin:    │
         │ 1. Find in users │
         │ 2. Check role    │
         │    === 'admin'   │
         │ 3. Check pass    │
         └────┬─────────────┘
              │
         ┌────┴──────────────────┐
         │                       │
      NO │                       │ YES
         │                       │
         ↓                       ↓
    ❌ Error            ✅ setCurrentUser()
    "Invalid           Save to sessionStorage
     credentials"      │
                       ↓
                /pages/admin-dashboard.html
                (Job management panel)
```

### Flow 3: Google Sign-In (NEW!)

```
┌──────────────────────────────────────┐
│    /pages/login.html or signup.html  │
│  [Google Sign-In Button]             │
└──────────────┬──────────────────────┘
               │
               ↓
        ┌──────────────────┐
        │ Google Auth      │
        │ Popup Opens      │
        └────┬─────────────┘
             │
             ├─ User selects Google account
             ├─ Authenticates
             │
             ↓
      ┌──────────────────┐
      │ Google returns   │
      │ JWT credential   │
      └────┬─────────────┘
           │
           ↓
    parseJwt() extracts:
    ├─ email
    ├─ name
    └─ picture (optional)
           │
           ↓
    Check if user exists:
    ├─ YES → Login user
    └─ NO  → Create new user
           │
           ↓
    setCurrentUser()
    Save to sessionStorage
           │
           ↓
    Redirect to dashboard
    (user or admin based on role)
```

---

## 🎯 Admin Job Posting Workflow

```
┌────────────────────────────────────────┐
│  Admin Dashboard                       │
│  /pages/admin-dashboard.html           │
└──────────┬───────────────────────────┘
           │
    ┌──────┴──────────────────────────┐
    │                                 │
    ↓                                 ↓
┌─────────────────────┐      ┌──────────────────┐
│ ➕ Post Job Tab    │      │ 📋 Manage Tab   │
│ (Form)              │      │ (Table)          │
└─────────────────────┘      └──────────────────┘
    │
    ├─ Fill Form:
    │  ├─ Job Title *
    │  ├─ Company *
    │  ├─ Category * (dropdown)
    │  ├─ Location *
    │  ├─ Job Type (default: Full-time)
    │  ├─ Apply Link * (URL)
    │  └─ Description *
    │
    ├─ Validate All Required Fields
    │  └─ If missing → ⚠️ Show warning
    │
    ↓
Create Job Object:
├─ id: 'j' + timestamp
├─ title, company, category, location
├─ type, applyLink, description
├─ status: 'approved' (auto-approve)
├─ postedBy: admin user ID
├─ createdAt: timestamp
└─ views: 0, applications: 0
    │
    ↓
saveJobs(jobs):
├─ Add to jobs array
├─ Save to localStorage
│  OR send to MongoDB API (if enabled)
└─ Update stats
    │
    ↓
┌──────────────────────┐
│ ✅ Success!          │
├──────────────────────┤
│ Notifications:       │
│ "Job posted: [name]" │
│                      │
│ Updates:             │
│ • Form clears        │
│ • Stats increment    │
│ • Appears in table   │
│ • On homepage        │
│ • Users can apply    │
└──────────────────────┘
```

---

## 📊 Admin Dashboard Layout

```
┌─────────────────────────────────────────────────────────────┐
│  JobSphere Admin Dashboard            [Refresh] [Home] [Logout]
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 📊 STATS GRID (Real-time)                               ││
│  │ ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ ││
│  │ │ Total:   │  │ Pending: │  │ Users:   │  │ Cats:    │ ││
│  │ │   15     │  │    2     │  │   128    │  │    7     │ ││
│  │ │          │  │          │  │          │  │          │ ││
│  │ │ Jobs     │  │ Approval │  │ Accounts │  │ Categories││
│  │ └──────────┘  └──────────┘  └──────────┘  └──────────┘ ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  TAB NAVIGATION:                                             │
│  [➕ Post Job] [📋 Manage] [⏳ Pending] [🏷️ Categories] [🔗 Links]
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐│
│  │  ➕ POST JOB TAB (Active)                                ││
│  │                                                          ││
│  │  Title:  [_________________________]                     ││
│  │  Company: [_________________________]                    ││
│  │  Category: [Select Category ▼]                          ││
│  │  Location: [_________________________]                   ││
│  │  Type: [Full-time ▼]                                    ││
│  │  Apply Link: [https://______________]                   ││
│  │  Description:                                            ││
│  │  [__________________________________]                  ││
│  │  [__________________________________]                  ││
│  │                                                          ││
│  │  [✅ Post Job] [🔄 Clear]                               ││
│  │                                                          ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  (Other tabs hidden)                                        │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow: Job Creation to Homepage

```
┌─────────────┐
│ Admin posts │
│ new job     │
└──────┬──────┘
       │
       ↓
┌──────────────────────┐
│ adminPostJob()       │
│ - Validate fields    │
│ - Create job object  │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────┐
│ saveJobs()           │
│ - Add to jobs[]      │
│ - Save to storage    │
└──────┬───────────────┘
       │
       ├─ localStorage
       │  └─ job_spare_jobs
       │
       └─ MongoDB (if enabled)
          └─ POST /api/jobs/save-all
       │
       ↓
┌──────────────────────┐
│ updateStats()        │
│ - Increment counter  │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────────┐
│ renderAllJobs()          │
│ - Show in Manage tab     │
│ - Table updates          │
└──────┬───────────────────┘
       │
       ↓
┌──────────────────────────┐
│ Home page reloads        │
│ renderFeaturedJobs()     │
│ - Shows 6 featured jobs  │
│ - Users see new job      │
│ - Users can apply        │
└──────────────────────────┘
```

---

## 🎭 Role-Based Access Control

```
┌────────────────────────────────────────────────┐
│          RBAC Decision Tree                    │
└────────────────────────────────────────────────┘

    User Logs In
         │
         ↓
    Check Role
         │
    ┌────┴────────────────┐
    │                     │
  "user"              "admin"
    │                     │
    ↓                     ↓
  Set user role      Set admin role
  Store in session   Store in session
    │                     │
    ↓                     ↓
Can Access:         Can Access:
✓ Browse jobs       ✓ Admin dashboard
✓ Search jobs       ✓ Post jobs
✓ View profile      ✓ Manage jobs
✓ Apply to jobs     ✓ Approve/reject
✗ Post jobs         ✓ Edit categories
✗ Manage jobs       ✓ Edit links
✗ Approve jobs      ✓ View stats
                    ✗ Browse jobs
                        (but can post them)
```

---

## 📱 Device Responsiveness

```
Desktop (1200px+)              Tablet (768px-1199px)      Mobile (< 768px)
┌──────────────────────┐      ┌─────────────────┐       ┌──────────┐
│ Header               │      │ Header          │       │ Header   │
├──────────────────────┤      ├─────────────────┤       ├──────────┤
│ Stats Grid (4 col)   │      │ Stats (2x2)     │       │ Stats    │
├──────────────────────┤      ├─────────────────┤       │ (stack)  │
│ Tab Navigation       │      │ Tab Navigation  │       ├──────────┤
│ (inline)             │      │ (scroll horiz)  │       │ Tabs     │
├──────────────────────┤      ├─────────────────┤       │ (scroll) │
│ Form (3 columns)     │      │ Form (1-2 cols) │       ├──────────┤
│ [Title] [Company]    │      │ [Title]         │       │ Form     │
│ [Category] [Loc]     │      │ [Company]       │       │ (stack)  │
│ [Type] [Link]        │      │ [Category]      │       │          │
├──────────────────────┤      │ [Location]      │       ├──────────┤
│ Info Panel (right)   │      ├─────────────────┤       │ (moved   │
│ (sidebar)            │      │ (stacked below) │       │ bottom)  │
└──────────────────────┘      └─────────────────┘       └──────────┘
```

---

## 🔐 Security Layers

```
┌────────────────────────────────────────────┐
│        Security Architecture               │
├────────────────────────────────────────────┤
│                                            │
│  Layer 1: Input Validation                 │
│  ├─ Required fields check                  │
│  ├─ Email format validation                │
│  ├─ URL validation                         │
│  └─ HTML escaping (XSS prevention)         │
│                                            │
│  Layer 2: Authentication                   │
│  ├─ Email/password verification            │
│  ├─ Admin role check                       │
│  ├─ Session management (sessionStorage)    │
│  └─ Google OAuth 2.0 (external)            │
│                                            │
│  Layer 3: Authorization                    │
│  ├─ Role-based access control              │
│  ├─ Admin-only endpoints                   │
│  └─ User-only endpoints                    │
│                                            │
│  Layer 4: Data Protection                  │
│  ├─ localStorage (encrypted by browser)    │
│  ├─ sessionStorage (cleared on close)      │
│  └─ MongoDB (encrypted at rest - future)   │
│                                            │
│  ⚠️ MISSING (for production):              │
│  ├─ Password hashing                       │
│  ├─ HTTPS encryption                       │
│  ├─ Rate limiting                          │
│  ├─ CSRF tokens                            │
│  └─ 2FA/MFA                                │
│                                            │
└────────────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture

```
Development          Staging              Production
┌─────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│ Localhost:5500  │  │ Staging URL      │  │ jobsphere.com    │
│ - localStorage  │  │ - MongoDB        │  │ - MongoDB Atlas  │
│ - No backend    │  │ - Backend API    │  │ - Backend (Node) │
│ - Testing       │  │ - Testing env    │  │ - HTTPS/SSL      │
│ - Quick dev     │  │ - Staging DB     │  │ - Production DB  │
└────────┬────────┘  └────────┬─────────┘  └────────┬─────────┘
         │                    │                     │
         ├─ Git push ────────→├─ Testing ──────────→├─ Live
         │                    │                     │
         ├─ Vercel ──────────→├─ Heroku ──────────→├─ Vercel
         │                    │                     │
         └─ npm dev ─────────→└─ npm run ─────────→└─ Production
                                    dev              build
```

---

## 📊 Database Schema (localStorage + MongoDB)

```
localStorage (Browser)          MongoDB (Server)
┌─────────────────────────┐    ┌─────────────────────────┐
│ job_spare_users         │    │ users collection        │
│ [                       │    │ {                       │
│  {                      │    │  _id: ObjectId,         │
│   id, name, email,      │    │  id, name, email,       │
│   password, role        │    │  password, role,        │
│  }                      │    │  createdAt              │
│ ]                       │    │ }                       │
│                         │    │                         │
│ job_spare_jobs          │    │ jobs collection         │
│ [                       │    │ {                       │
│  {                      │    │  _id: ObjectId,         │
│   id, title, company,   │    │  id, title, company,    │
│   category, location,   │    │  category, location,    │
│   type, applyLink,      │    │  type, applyLink,       │
│   description, status,  │    │  description, status,   │
│   postedBy, createdAt   │    │  postedBy, createdAt    │
│  }                      │    │ }                       │
│ ]                       │    │                         │
│                         │    │ categories collection   │
│ job_spare_categories    │    │ {                       │
│ [                       │    │  _id: ObjectId,         │
│  "category1",           │    │  name                   │
│  "category2"            │    │ }                       │
│ ]                       │    │                         │
└─────────────────────────┘    └─────────────────────────┘
```

---

## ✨ Feature Completeness Diagram

```
Core Features              Extended Features        Future Enhancements
├─ ✅ User Login           ├─ ✅ Job Categories     ├─ ⏳ 2FA Authentication
├─ ✅ Admin Login          ├─ ✅ Apply Links        ├─ ⏳ Email Notifications
├─ ✅ Google OAuth         ├─ ✅ Job Search         ├─ ⏳ Saved Jobs
├─ ✅ Job Posting          ├─ ✅ Admin Stats        ├─ ⏳ Application History
├─ ✅ Job Management       ├─ ✅ Dark Mode          ├─ ⏳ Skill Recommendations
├─ ✅ User Dashboard       ├─ ✅ Responsive UI      ├─ ⏳ Analytics Dashboard
└─ ✅ Admin Dashboard      └─ ✅ Toast Notify       └─ ⏳ Advanced Filtering
```

---

**This complete visual guide shows how all components work together!** 🎯

