# Environment Configuration Template
# Copy this file to .env and fill in your values
# NEVER commit .env file to version control - add to .gitignore

# ======================
# Database Configuration
# ======================

# MongoDB Connection String (from MongoDB Atlas)
# Format: mongodb+srv://username:password@cluster-name.xxxxx.mongodb.net/database-name?retryWrites=true&w=majority
MONGODB_CONNECTION_STRING=mongodb+srv://your_username:your_password@jobsphere-cluster.xxxxx.mongodb.net/jobsphere?retryWrites=true&w=majority

# MongoDB Atlas Database Name
MONGODB_DATABASE=jobsphere

# ======================
# Server Configuration
# ======================

# Server Port
PORT=3000

# Environment (development, staging, production)
NODE_ENV=development

# ======================
# Frontend Configuration
# ======================

# Frontend URL (for CORS and redirects)
FRONTEND_URL=http://localhost:5500

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# ======================
# API Configuration
# ======================

# API Base URL (frontend uses this)
API_BASE_URL=http://localhost:3000/api

# API Request Timeout (milliseconds)
API_TIMEOUT=30000

# ======================
# Security Configuration
# ======================

# JWT Secret for token signing (generate a random string)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# JWT Token Expiry
JWT_EXPIRY=7d

# CORS Origin (comma-separated for multiple origins)
CORS_ORIGIN=http://localhost:5500,http://localhost:3000

# Rate Limiting (requests per 15 minutes)
RATE_LIMIT_REQUESTS=100

# ======================
# Email Configuration (Optional)
# ======================

# Email Service (e.g., sendgrid, mailgun, smtp)
EMAIL_SERVICE=smtp

# Email Address (from address)
EMAIL_FROM=noreply@jobsphere.com

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password

# ======================
# Logging Configuration
# ======================

# Log Level (error, warn, info, debug, trace)
LOG_LEVEL=info

# Log File Location
LOG_FILE=logs/app.log

# ======================
# Feature Flags
# ======================

# Enable/Disable Features
ENABLE_EMAIL_VERIFICATION=false
ENABLE_JOB_APPROVAL=true
ENABLE_ANALYTICS=false

# ======================
# Third-Party Services
# ======================

# Analytics (e.g., Google Analytics ID)
ANALYTICS_ID=UA-XXXXXXXXX-X

# Sentry Error Tracking (optional)
SENTRY_DSN=

# ======================
# Notes for Setup
# ======================

# 1. MongoDB Connection String:
#    - Get from MongoDB Atlas → Connect → Application
#    - Replace <username>, <password>, and cluster name
#    - Database name should match your setup
#
# 2. Google OAuth:
#    - Create at: https://console.cloud.google.com
#    - OAuth 2.0 → Credentials → Web Application
#    - Authorized redirect URIs: http://localhost:3000/auth/callback
#
# 3. JWT Secret:
#    - Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#    - Use unique value for production
#    - Never expose in logs or version control
#
# 4. SMTP Configuration:
#    - For Gmail: Enable 2FA, create App Password (16 chars)
#    - For other providers: Check their documentation
#
# 5. Frontend Configuration:
#    - FRONTEND_URL used for redirects after auth
#    - API_BASE_URL used by frontend to call backend
#    - Make sure both are correct for your deployment
